CREATE TABLE [dbo].[SopsSpecialty] (
    [SopsSpecialtyId] INT            IDENTITY (1, 1) NOT NULL,
    [SpecialtyType]   NVARCHAR (200) NOT NULL,
    PRIMARY KEY CLUSTERED ([SopsSpecialtyId] ASC)
);

